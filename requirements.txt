# What RENDER installs. Deliberately no torch, no ultralytics.
# Use Python 3.12 on Linux so pip resolves mediapipe 0.10.35 instead of the
# older Python 3.11 wheel that pulls JAX/JAXLIB.

fastapi
uvicorn[standard]
python-multipart          # UploadFile needs this. FastAPI won't tell you nicely.
numpy
mediapipe==0.10.35        # Pulls opencv-contrib-python; do not install a second cv2 wheel.
