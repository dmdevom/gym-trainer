# The default browser client uses the same-origin /backend-api rewrite.
BACKEND_API_URL=https://gym-trainer-production-a2c6.up.railway.app

# Optional: call a CORS-enabled backend directly instead of using the rewrite.
# NEXT_PUBLIC_API_BASE_URL=https://gym-trainer-production-a2c6.up.railway.app
